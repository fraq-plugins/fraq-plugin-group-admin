# Cockpit - fraq

Focus:
Maintain and release the reusable `fraq-plugin-group-admin` moderation package for current Fraq versions.

## In flight
- group-admin - package `1.0.2` is published and the test deployment remains on `1.0.1`; local `1.0.3` adds Fraq market category `management` and is ready to publish. Smoke has a pre-existing help convention assertion mismatch.

## Next
- Publish `1.0.3`, then verify its Fraq market listing under `management`.
- Update the test deployment from `1.0.1` to `1.0.2` when requested.
- Configure deployment `groupIds` and opt-in automatic tasks before upgrading a running bot.

## Open questions
- Should the minimum QQ level or rejection reason vary by group?
